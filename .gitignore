# dependencies
node_modules

# next.js
.next
out

# production
build

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# misc
.DS_Store

# build artifacts
tsconfig.tsbuildinfo

# operational DB maintenance scripts — contém PII (CPF/nomes de alunos).
# NUNCA versionar: mantém local apenas (LGPD).
scripts/
